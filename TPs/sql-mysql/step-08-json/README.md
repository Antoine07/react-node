# Step 08 — JSON (MySQL)

## Pré-requis

Exécuter le schéma + seed:
- `../step-02-schema-ddl/schema.sql`
- `../step-03-select-basics/seed.sql`

## Objectif

Ajouter une colonne JSON sur `posts` et écrire des requêtes.

On va stocker dans `posts.meta`:
- `tags`: tableau de strings
- `readingTime`: nombre (minutes)

## À faire

1) Compléter et exécuter `setup.sql` (ALTER TABLE + quelques UPDATE)
2) Compléter `exercises.sql`

