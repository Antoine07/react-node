# Step 04 — CRUD `posts`

## À faire

Implémenter le CRUD:
- `GET /posts`
- `GET /posts/:id`
- `POST /posts`
- `PUT /posts/:id`
- `DELETE /posts/:id`

Contrainte:
- stockage en mémoire (tableau)
- `id` auto-incrémenté
- `datetime` = ISO string à la création

## Lancer

```bash
npm run dev
```

